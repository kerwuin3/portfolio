

/* El Page es el componente que se renderiza en la ruta raíz, se puede usar para agregar elementos específicos de la página, como el contenido principal, etc. */

export default function Home() {
  return (
    <div>
      <main>
        
      </main>
    </div>
  );
}
